rootProject.name = 'microservices'

def modules = [
    'resource-service',
    'song-service',
    'service-registry',
    'resource-processor',
    'e2e-tests',
    'api-gateway'
]

modules.each { moduleName ->
    if (file(moduleName).exists()) {
        include(moduleName)
    }
}
