rootProject.name = 'microservices'

def modules = [
    'resource-service',
    'song-service',
    'service-registry',
    'resource-processor'
]

modules.each { moduleName ->
    if (file(moduleName).exists()) {
        include(moduleName)
    }
}
